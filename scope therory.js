 // -----------------------------scope----------------------------------------------
    //Scope determines the accessibility and visibility of variables.

    //----------------------------types of scoope-------------------------------------
    //there are 3 ty[es of scope in java script.
    //1.global scope 2.local or functional scope 3.script scope or block scope.

    //1.----------------------global scope-----------------------------------
   // if we declared a  var variable globally (outside of any function) have a global scope.

   //if we declared a let and const variables globally have a script scope.


   //2.-----------------------Local scope------------------------------------------
   //if we decalared a variables(let ,const ,var) within a function have a local scope.

   //3.------------------------Block scope----------------------------------------
   //if we decalared a variables(let const and var) inside a {}block then let and const have a block scope and var have a global scope

   //----------------------------lifetime----------------------------------
  // local scope will be end or dead when the function is completed

  //---------------------------------Accedenatal variables--------------------------
 // if we dont declared a variable so it will automatically become a global variable